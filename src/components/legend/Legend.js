import React from "react";
import PropTypes from "prop-types";

import { DEFAULT_COLORS } from "../../assets/theme/colors";

export const Legend = ({ data, colors }) => {
  return <div>ggwp no re</div>;
};

Legend.propTypes = {
  data: PropTypes.array,
  colors: PropTypes.array,
};
